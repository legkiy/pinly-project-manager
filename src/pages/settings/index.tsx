import { LocaleSwitcher, ThemeSwitcher } from '@/features';

const SettingsPage = () => {
  return (
    <div>
      SettingsPage
      <ThemeSwitcher />
      <LocaleSwitcher />
    </div>
  );
};
export default SettingsPage;
